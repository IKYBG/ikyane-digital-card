import { DashboardHeader } from '@/components/dashboard/DashboardHeader'; import { LinksManager } from '@/components/dashboard/LinksManager'; import { getCurrentQard } from '@/lib/qard/data';
export default async function LinksPage() { const { profile, links } = await getCurrentQard(); return <><DashboardHeader eyebrow="Contacts" title="Tous tes liens, dans le bon ordre." description="Ajoute, masque et réorganise chaque moyen de te joindre." /><LinksManager profile={profile} initialLinks={links} /></>; }
