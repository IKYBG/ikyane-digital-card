import type { Profile } from '@/types/database';
const escape = (value: string) => value.replace(/\\/g, '\\\\').replace(/\n/g, '\\n').replace(/,/g, '\\,').replace(/;/g, '\\;');
export function createVCard(profile: Profile) { const name = escape(profile.display_name); const lines = ['BEGIN:VCARD','VERSION:3.0',`FN:${name}`,`N:${escape(profile.last_name ?? '')};${escape(profile.first_name ?? '')};;;`]; if (profile.phone_public) lines.push(`TEL;TYPE=CELL:${escape(profile.phone_public)}`); if (profile.email_public) lines.push(`EMAIL;TYPE=INTERNET:${escape(profile.email_public)}`); if (profile.company) lines.push(`ORG:${escape(profile.company)}`); if (profile.job_title) lines.push(`TITLE:${escape(profile.job_title)}`); if (profile.website) lines.push(`URL:${escape(profile.website)}`); lines.push('END:VCARD'); return `${lines.join('\r\n')}\r\n`; }
